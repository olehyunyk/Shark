# Neon (Vercel + Shark_* або DATABASE_URL)
DATABASE_URL=postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require

# Jira — лише облікові дані (URL і борди в src/config/jira-boards.ts)
JIRA_EMAIL=you@sharkscode.com
JIRA_API_TOKEN=

# Опційно перевизначити JQL (за замовчуванням — MK з config)
# JIRA_JQL=project = MK AND statusCategory != Done ORDER BY duedate ASC
